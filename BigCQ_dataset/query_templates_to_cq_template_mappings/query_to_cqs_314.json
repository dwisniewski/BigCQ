{
    "cqs": [
        "which objects are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what objects are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "what is equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "what objects are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what categories are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "what entities are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "which objects are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which entities are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which entities are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "which things are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "which categories are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "which things are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what things are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "which categories are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "which things are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which entities are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "what are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "which objects are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "what categories are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what things are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "what objects are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "what categories are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "what things are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what entities are the same as c2 that dp1 dt1 or that dp2 dt2?",
        "which is identical with c2 that dp1 dt1 or that dp2 dt2?",
        "what is the same as c2 that dp1 dt1 or that dp2 dt2?",
        "what entities are equal to c2 that dp1 dt1 or that dp2 dt2?",
        "what is identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which categories are identical with c2 that dp1 dt1 or that dp2 dt2?",
        "which is the same as c2 that dp1 dt1 or that dp2 dt2?",
        "which is equal to c2 that dp1 dt1 or that dp2 dt2?",
        "which are the same as c2 that dp1 dt1 or that dp2 dt2?"
    ],
    "query": "SELECT ?x WHERE { ?x owl:equivalentClass [ owl:intersectionOf ( <c2> [ owl:unionOf ( [ rdf:type owl:Restriction ; owl:onProperty <dp1> ; owl:hasValue <dt1> ] [ rdf:type owl:Restriction ; owl:onProperty <dp2> ; owl:hasValue <dt2> ] ) ] ) ] . }"
}